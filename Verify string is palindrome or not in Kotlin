fun main() {
  println("Plaindrome--- ${isPalindrome("radar")}")
}

fun isPalindrome(str : String) : Boolean{
  	var i = 0 
    var j = str.length-1
    var flag = true
    
    while(i<j){
        if(str.get(i) != str.get(j))
        flag = false
        break
    }
    return flag
}
